<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Passing_Intents" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Passing_Intents" parent="Base.Theme.Passing_Intents" />

    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_clear</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>